type Company {
  id: ID!
  name: String!
}

input CompanyInput {
  id: ID!
  name: String!
}

type Query {
  findAll: [Company]!
  findOne(id: ID!): Company
}

type Mutation {
  create(company: CompanyInput!): Company!
  update(id: ID!, company: CompanyInput!): Company!
  delete(id: ID!): Company
}